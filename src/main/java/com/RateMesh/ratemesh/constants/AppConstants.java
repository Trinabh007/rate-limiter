package com.RateMesh.ratemesh.constants;

import java.util.UUID;

public class AppConstants {
    private AppConstants() {} // prevent instantiation
    
    public static final String INSTANCE_ID = UUID.randomUUID().toString();
}
